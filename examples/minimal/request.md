# Normalize greeting names

Ensure the greeting function trims surrounding whitespace, rejects blank names with a clear `ValueError`, and preserves the existing `Hello, NAME!` output. Keep the application dependency-free and cover the behavior with unit tests.
