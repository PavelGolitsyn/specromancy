# specromancy

Toolkit for Spec-Driven Development (SDD).

The first harness-agnostic pipeline proof of concept lives in
[`POC_v2/`](POC_v2/README.md). It models a workflow as a small state machine,
loads each stage from a portable `SKILL.md`, and keeps vendor-specific command
surfaces outside the workflow definition.
