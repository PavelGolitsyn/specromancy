---
name: pipeline
description: Use when orchestrating the test workflow.
license: MIT
compatibility: Specromancy pipeline schema version 1.
metadata:
  role: orchestrator
---

# Pipeline

Run `bin/specromancy status RUN_ID --json`. You must never edit `run.json` or
`events.jsonl`.
